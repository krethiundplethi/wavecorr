bin_PROGRAMS = wavecorr

# set the include path found by configure
INCLUDES= $(all_includes)

# the library search path.
wavecorr_LDFLAGS = $(all_libraries) 
wavecorr_SOURCES = wavecorr.cpp
wavecorr_LDADD = -lsndfile -litpp
